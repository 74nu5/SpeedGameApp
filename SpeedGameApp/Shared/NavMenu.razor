<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">SpeedGameApp</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="this.ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@(this.NavMenuCssClass) nav-scrollable" @onclick="this.ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <i class="bi bi-plus-circle fs-4 mx-3"></i> Créer une partie
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="admin">
                <i class="bi bi-gear-wide-connected fs-4 mx-3"></i> Admin
            </NavLink>
        </div>       
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="admin/questions">
                <i class="bi bi-question-square fs-4 mx-3"></i> Questions
            </NavLink>
        </div>
    </nav>
</div>

@code {

    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => this.collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        this.collapseNavMenu = !this.collapseNavMenu;
    }

}